import React, { FC } from 'react'
import { Container } from '@mui/material'

const LoginLayout: FC<React.PropsWithChildren<unknown>> = ({ children }) => <Container>{children}</Container>

export default LoginLayout
